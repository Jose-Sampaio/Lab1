//Exemplo Stack 1
#include <iostream>
#include <stack>
using namespace std;

int main()
{
	stack <int> pilha;
	int n;
	for (int i = 0; i < 4; ++i)
	 {
	 	cin>>n;
	 	pilha.push(n);
	 } 

	 while (!pilha.empty()){
	 	cout<<pilha.top()<<endl;
	 	pilha.pop();
	 }
	
	return 0;
}
----------------------------------------------------------------------------------------------------------------------------

